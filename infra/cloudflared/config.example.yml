# Create the named Tunnel in your Cloudflare account first, then replace both placeholders.
tunnel: REPLACE_WITH_TUNNEL_UUID
credentials-file: /Users/peiyuancai/.cloudflared/REPLACE_WITH_TUNNEL_UUID.json

ingress:
  - hostname: api-dev.bonfirework.org
    service: http://127.0.0.1:8080
    originRequest:
      noTLSVerify: false
  - service: http_status:404
